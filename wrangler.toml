name = "ai-lens"
compatibility_date = "2026-09-09"
pages_build_output_dir = "."

# Stores session observations (167-signal audit results tagged by session id)
# so /api/session/compare can diff a BEFORE session against an AFTER session.
# Create the namespace, then paste its real id below — see README "Session storage" section.
[[kv_namespaces]]
binding = "AI_LENS_SESSIONS"
id = "REPLACE_WITH_REAL_KV_NAMESPACE_ID"
